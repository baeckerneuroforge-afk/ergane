/** Hard cap on the pending-approvals list (hot path every navigation-related load). */
export const APPROVALS_PENDING_LIMIT = 100;

/** Hard cap on the recent decided-approvals table. */
export const APPROVALS_DECIDED_LIMIT = 20;
