/** Hard cap on the clients hub list page. */
export const CLIENTS_PAGE_LIMIT = 200;

/** Hard cap on runs shown on a client detail page. */
export const CLIENT_DETAIL_RUNS_LIMIT = 50;

/** Hard cap on artifacts shown on a client detail page. */
export const CLIENT_DETAIL_ARTIFACTS_LIMIT = 50;
